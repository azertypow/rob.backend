title: Tag

icon: tag

num: zero

status:
  draft:
    label: actif
    text: Un tag n'a pas de status, il est donc toujours actif

options:
  delete: false
  changeStatus: false

columns:
  - width: 2/3
    fields:
      info:
        type: info
        label: ' '
        text: |
          La référence de ce champs est **{{page.uuid}}**, et toutes les pages qui y sont liées garde cette référence.
